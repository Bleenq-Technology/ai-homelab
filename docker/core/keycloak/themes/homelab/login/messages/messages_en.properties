# homelab login theme overrides (merged over parent keycloak.v2 messages).
# Cleaner default passkey label, since the "name your passkey" prompt is suppressed
# (see webauthn-register.ftl).
webauthn-registration-init-label=Passkey
